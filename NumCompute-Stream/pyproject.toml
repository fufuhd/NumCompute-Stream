[project]
name = "numcompute-stream"
version = "0.1.0"
description = "A NumPy-based streaming decision tree and ensemble framework"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
